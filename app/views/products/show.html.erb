<h1><%= @product.title %></h1>
<p><%= @product.description %></p>

<p>Price: <%= @product.price %></p>
<%= link_to 'Back to Products', products_path %> | <%= link_to 'Delete', product_path(@product), method: :delete, data: { confirm: 'Are you sure?' } %>